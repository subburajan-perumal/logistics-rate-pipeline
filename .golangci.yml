version: "2"
linters:
  default: standard
  enable: [misspell, bodyclose, errorlint, gocritic]
  settings:
    errcheck:
      exclude-functions:
        - (*os.File).Close
        - (io.Closer).Close
        - (net/http.ResponseWriter).Write
formatters:
  enable: [gofmt]
